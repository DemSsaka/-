# Video Notes (3-5 min)

- Show landing page, login/register, and dashboard.
- Create a wishlist and add/edit/archive items.
- Copy public link and open it in incognito.
- Reserve an item from one tab and show realtime update in another tab.
- Add contribution and show aggregate progress update in realtime.
- Show that owner only sees anonymous reserved state + aggregate collected amount.
- Demonstrate reconnect badge by temporarily disabling backend WS and show polling fallback.
- Show OG parse endpoint usage and explain SSRF blocking + cache.
- Show tests: reservation race, SSRF tests, contribution validation, frontend smoke.
- Briefly walk through architecture (Next.js + FastAPI + Postgres + Alembic + WS).
